Implement `Convert<S, T, U>` which takes three strings and convert `T` to `U` if `S` contains `T`.
For example

```
type HyphenToUnderScore = Convert<'Content-Type', '-', '_'> // expected to be 'Content_Type'
```
