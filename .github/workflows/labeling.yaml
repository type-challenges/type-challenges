name: Labeling

on:
  issues:
    types: [opened, edited]

jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: node -r esm -r esbuild-register ./actions/loader labeling