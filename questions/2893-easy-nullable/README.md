Implement the generic ```Nullable<T>``` which makes ```T```   nullable (contains null and undefined )

For example

```typescript
type nullableString = Nullable<string> // string | null | undefined
```
