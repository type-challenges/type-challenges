name: close issue

on:
  issues:
    types: [closed, reopened]

jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/github-script@v2
        with:
          script: |
            const script = require(`${process.env.GITHUB_WORKSPACE}/actions/responsive-in-issue.js`)
            await script(github, context, core)
